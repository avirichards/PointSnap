import { SearchWorkspace } from "@/components/search/search-workspace";
export const metadata = { title: "Find award flights" };
export default function SearchPage() {
  return <SearchWorkspace />;
}
